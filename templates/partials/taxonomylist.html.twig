{% set taxlist = taxonomylist.get() %}

{% if taxlist %}

<span class="tags">
    {% for tax,value in taxlist[taxonomy] %}

	<a href="{{ blog_url }}/tag{{ config.system.param_sep }}{{ tax|e('url') }}" class="uk-badge">{{ tax }}</a>

    {% endfor %}
</span>
{% endif %}
